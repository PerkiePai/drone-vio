%YAML:1.0
# OpenVINS config for Isaac Sim nadir drone dataset.
# Monocular, IMU (FRD body, 50 Hz) + downward camera (10 Hz, 960x600).
#
# IMPORTANT: camera is gimbal-stabilised (extrinsic_is_constant=false).
# The T_imu_cam in kalibr_imucam_chain.yaml is the takeoff snapshot only.
# calib_cam_extrinsics=true lets OpenVINS refine it slightly, but large
# attitude changes will cause drift.  Re-record with DOWN_STABILIZE=False
# for a proper rigid extrinsic before using this in production.

verbosity: "INFO"

use_fej: true
integration: "rk4"
use_stereo: false
max_cameras: 1

calib_cam_extrinsics: true
calib_cam_intrinsics: true
calib_cam_timeoffset: true
calib_imu_intrinsics: false
calib_imu_g_sensitivity: false

max_clones: 11
max_slam: 50
max_slam_in_update: 25
max_msckf_in_update: 40
dt_slam_delay: 1

gravity_mag: 9.81

feat_rep_msckf: "GLOBAL_3D"
feat_rep_slam: "ANCHORED_MSCKF_INVERSE_DEPTH"
feat_rep_aruco: "ANCHORED_MSCKF_INVERSE_DEPTH"

# ZUPT: disabled — drone rises immediately from frame 1, never fully static
try_zupt: false
zupt_chi2_multipler: 1
zupt_max_velocity: 0.3
zupt_noise_multiplier: 10
zupt_max_disparity: 1.0
zupt_only_at_beginning: true

# Barometer altitude update — constrains scale in nadir (low-parallax) regime.
# /baro_height topic is published by the bag converter.
use_baro: true
baro_noise_std: 0.3         # sim baro is clean; 0.3 m 1-sigma
baro_chi2_multiplier: 1.0

# ==================================================================
# Initialization. Drone rises immediately from frame 1 at ~83 cm/s.
# At 0.5 m altitude, feature disparity reaches ~68 px/frame — far above
# the static-init limit.  Use dynamic init as primary path.
init_window_time: 2.0
init_imu_thresh: 1.5
init_max_disparity: 80.0    # allow large disparity (rising nadir camera)
init_max_features: 75

# dynamic init: works during motion (drone rises immediately, no static period)
init_dyn_use: true
init_dyn_mle_opt_calib: false
init_dyn_mle_max_iter: 50
init_dyn_mle_max_time: 0.05
init_dyn_mle_max_threads: 6
init_dyn_num_pose: 6
init_dyn_min_deg: 10.0
init_dyn_inflation_ori: 10
init_dyn_inflation_vel: 100
init_dyn_inflation_bg: 10
init_dyn_inflation_ba: 100
init_dyn_min_rec_cond: 1e-12
init_dyn_bias_g: [0.0, 0.0, 0.0]
init_dyn_bias_a: [0.0, 0.0, 0.0]

# ==================================================================
record_timing_information: false
record_timing_filepath: "/tmp/traj_timing.txt"

save_total_state: true
filepath_est: "/tmp/ov_estimate.txt"
filepath_std: "/tmp/ov_estimate_std.txt"
filepath_gt: "/tmp/ov_groundtruth.txt"

# ==================================================================
# Front-end: KLT on 960x600 nadir imagery
use_klt: true
num_pts: 300
fast_threshold: 15          # ground texture varies; lower threshold helps
grid_x: 5
grid_y: 3                   # 960x600: fewer rows
min_px_dist: 10
knn_ratio: 0.70
track_frequency: 10.0
downsample_cameras: false
num_opencv_threads: 4
histogram_method: "CLAHE"   # outdoor lighting in sim

use_aruco: false
num_aruco: 1024
downsize_aruco: true

# ==================================================================
up_msckf_sigma_px: 1
up_msckf_chi2_multipler: 1
up_slam_sigma_px: 1
up_slam_chi2_multipler: 1
up_aruco_sigma_px: 1
up_aruco_chi2_multipler: 1

use_mask: false

relative_config_imu: "kalibr_imu_chain.yaml"
relative_config_imucam: "kalibr_imucam_chain.yaml"
